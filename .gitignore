__pycache__/
*.pyc
.verify-out/
mw-verify-*/
# raw proprietary HBR source files and marketing assets — do not publish
*.xlsx
*.xls
Ads/
Masterplans/
# anchored: Git matches ignore patterns case-insensitively on Windows, so a bare
# "ReadyNow/" also swallows tools/readynow/ (the pipeline itself, which must be tracked)
/ReadyNow/
/backups/

# converter working dir
tools/_work/

# internal docs: security audit, white paper, pitch one-pager (keep out of public Pages repo)
docs/
